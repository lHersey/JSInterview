/**
 * Find a pair of elements from an array whose sum equals a given number.
 *
 * Example:
 * sumPair([1,5,10], 11) true
 * sumPair([-5, 10], 5) true
 * sumPair([1,3,6,2], 3) true
 * sumPair([1,6,3,6], 2) false
 * @param {Array<number>} arr
 * @param {number} target
 * @returns {boolean}
 */
function sumPair(arr, target) {
  //TODO
  return { arr, target };
}

//sumPair([1,5,10], 15) true
//sumPair([-5, 10], 5) true

module.exports = sumPair;
