/**
 * Being a=1, b=2, c=3, d=4... z=26, and given a encoded string like '12',
 * return the number of ways to decode that string
 *
 * Example:
 *    for '12' return 2
 *    for '226' returns 3
 *    for '1234' returns 3
 * @param {*} str
 */
function decodeMessage(str) {
  //TODO
  return str;
}

module.exports = decodeMessage;
